- add git/github source support to package.json (and install command)
- add zip/tarball url/filepath support to package.json (and install command)
- wrap non-amd code on the fly (see how volo does this)

- see how volo manages to be loader agnostic
- see how component manages things other than js (css, html...)

- add private repo couchapp to website
- fork bbb and add integration





- Travis CI

- add command-line api (eg, --json option) to make it easier to integrate
  with non-nodejs frameworks/tools

- add integration tests for pack

- add documentation for baseurl option added to many commands

- make sure the example uses in install command help actually work
  (eg, path/to/package.json)
